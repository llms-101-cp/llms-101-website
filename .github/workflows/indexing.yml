name: Auto-Generate Content Indexes

on:
  push:
    branches:
      - main
    paths:
      - 'content/articles/**'
      - 'content/reports/**'

permissions:
  contents: write

jobs:
  build-index:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Index Generator
        run: node scripts/generate-indices.js

      # ── FORCE COMMIT AND PUSH LAYER ──
      # We removed the conditional check so this step runs NO MATTER WHAT
       Kakao
      - name: Commit and Push Indexes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Stage the freshly generated index files
          git add content/articles_index.json content/reports_index.json
          
          # Commit the files. If git complains there are no changes, || exit 0 lets it pass gracefully
          git commit -m "chore: auto-regenerate content indexes" || echo "No structural changes to commit"
          
          # Force the push straight into your main branch line
          git push origin main
